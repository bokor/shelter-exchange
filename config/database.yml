defaults: &defaults
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 5
  username: root
  password:
  host: localhost

development:
  <<: *defaults
  database: shelterexchange_dev

test:
  adapter: sqlite3
  encoding: utf8
  database: ":memory:"

#test:
  #<<: *defaults
  #database: shelterexchange_test

