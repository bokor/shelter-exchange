module PlacementsHelper
  # 
  # def setup_comments(placement)
  #   placement.comments.build if placement.comments.blank? || !placement.comments.map(&:new_record?).any? 
  #   placement 
  # end
  # 
end
