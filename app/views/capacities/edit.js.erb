$('#capacity_<%=@capacity.id%>').hide();
$('#capacity_<%=@capacity.id%>').after( "<%= escape_javascript(render 'edit_capacity', :collection => @capacity) %>" );
$('#edit_capacity_<%=@capacity.id%>').show();

// Google Anayltics
<% if Rails.env.production? %>
  _gaq.push(['_trackPageview', "/capacities/{id}/edit"]);
<% end %>

// Discard Flash Message
<% flash.discard %>

