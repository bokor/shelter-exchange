$('#note_<%=@note.id%>').hide();
$('#note_<%=@note.id%>').after("<%= escape_javascript(render 'edit_note', :collection => @note) %>");
$('#edit_note_<%=@note.id%>').show();

// Google Anayltics
<% if Rails.env.production? %>
  _gaq.push(['_trackPageview', "/notes/{id}/edit"]);
<% end %>

// Discard Flash Message
<% flash.discard %>

