$('#transfer_<%=@transfer.id%>').hide();
$('#transfer_<%=@transfer.id%>').after("<%= escape_javascript(render 'edit_transfer', :collection => @transfer) %>");
$('#edit_transfer_<%=@transfer.id%>').show();

// Google Analytics
<% if Rails.env.production? %>
  _gaq.push(['_trackPageview', "/transfers/{id}/edit"]);
<% end %>

// Discard Flash Message
<% flash.discard %>

