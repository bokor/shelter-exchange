$('#transfer_<%= @transfer.id %>').fadeOut(1000, function() {
	$(this).remove();
});

// Google Analytics
<% if Rails.env.production? %>
  _gaq.push(['_trackPageview', "/transfers/{id}/destroy"]);
<% end %>

// Discard Flash Message
<% flash.discard %>

