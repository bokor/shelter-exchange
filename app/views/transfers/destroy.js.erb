$('#transfer_<%= @transfer.id %>').fadeOut(1000, function() {
	$(this).remove();
});
<% flash.discard %>

// Google Analytics
if (typeof(_gaq) != "undefined") {  _gaq.push(['_trackPageview', "/transfers/{id}/destroy"]); }

