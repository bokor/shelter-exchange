$('#comment_<%=@comment.id%>').hide();
$('#comment_<%=@comment.id%>').after("<%= escape_javascript(render 'edit_comment', :collection => @comment) %>");
$('#edit_comment_<%=@comment.id%>').show();

// Google Anayltics
<% if Rails.env.production? %>
  _gaq.push(['_trackPageview', "/comments/{id}/edit"]);
<% end %>

// Discard Flash Message
<% flash.discard %>

