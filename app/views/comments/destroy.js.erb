$('#comment_<%= @comment.id %>').fadeOut(1000, function() {
	$(this).remove();
});

// Google Anayltics
<% if Rails.env.production? %>
  _gaq.push(['_trackPageview', "/comments/{id}/destroy"]);
<% end %>

// Discard Flash Message
<% flash.discard %>

