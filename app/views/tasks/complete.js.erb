$('#task_<%= @task.id %>').fadeOut(1000, function() {
	$(this).remove();
	Tasks.removeTaskSection();
});

// Google Analytics
<% if Rails.env.production? %>
  _gaq.push(['_trackPageview', "/tasks/{id}/complete"]);
<% end %>

// Discard Flash Message
<% flash.discard %>

