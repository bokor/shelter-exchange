<%= title "Intake Monthly Total by Animal Type - Reports"  %>

<%= content_for :javascripts do %>
	<%= include_javascripts :reports_s3 %>
	<script type="text/javascript">
		$(function() {
			Reports.barChart('Intake Monthly Total By Type', 'intake_monthly_total_by_type', 'Intake');
		});
	</script>
<% end %>

<%= content_for :page_content do %>
	
	<div class="page_heading blue">
		<h1>Reports</h1>
		<div class="action_links">
			<%= label_tag :report_selected_year, "Select year" %>
			<%= select_year(Date.today, {:field_name => :report_selected_year, :start_year => current_year - 3, :end_year => current_year}, {:onchange => "Reports.barChart('Intake Monthly Total By Type', 'intake_monthly_total_by_type', 'Intake');"})%>
		</div>
	</div>
	
	<div class="page_content">
		<div id="intake_monthly_total_by_type" class="report"></div>
	</div>

<% end %>

<%= content_for :sidebar do %>
	<%= render "sidebar" %>
<% end %>