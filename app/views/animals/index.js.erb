<% unless @animals.blank? %>
	$('#animals').html( "<%= escape_javascript( render 'animal_list', :animals => @animals) %>" );
<% else %>
	$('#animals').html( "<%= escape_javascript( render 'no_animals' ) %>" );
<% end %>
