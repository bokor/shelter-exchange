$('#alert_<%= @alert.id %>').fadeOut(1000, function() {
	$(this).remove();
	Alerts.removeAlertSection();
});

<% flash.discard %>

// Google Anayltics
if (typeof(_gaq) != "undefined") { _gaq.push(['_trackPageview', "/alerts/{id}/stop"]); }
