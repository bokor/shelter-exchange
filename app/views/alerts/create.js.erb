//$('alert-errors').update( "<%= escape_javascript(render 'global/error_messages', :target => @alert) %>" );
<% unless @alert.errors.any? %>
$('#alerts').prepend("<%= escape_javascript(render @alert) %>"); 
$(".alert").first().effect("highlight", {}, 1000);
$('#new_alert').get(0).reset();
<% end %>