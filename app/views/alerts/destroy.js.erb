$('#alert_<%= @alert.id %>').fadeOut(1000, function() {
	$(this).remove();
	Alerts.removeAlertSection();
});

// Google Anayltics
<% if Rails.env.production? %>
  _gaq.push(['_trackPageview', "/alerts/{id}/destroy"]);
<% end %>

// Discard Flash Message
<% flash.discard %>

