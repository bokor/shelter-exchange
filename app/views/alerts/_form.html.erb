<li>
	<%= f.label :title, "Title <span class='req'>*</span>".html_safe, :class => "desc" %>
	<%= f.text_field :title, :class => "field text large" %>
</li>
<li>
	<%= f.label :severity, "Severity <span class='req'>*</span>".html_safe, :class => "desc" %>
	<%= f.collection_select :severity, Alert::SEVERITY, :to_s, :humanize, :prompt => "Select one", :class => "field select medium" %>
</li>
<li class="full">
	<%= f.label :description, "Description", :class => "desc" %>
	<%= f.text_area :description, :class => "field textarea medium" %>
</li>	
