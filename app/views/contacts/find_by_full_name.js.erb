<% unless @contacts.blank? %>
	$('#contacts').html( "<%= escape_javascript( render 'contact_list', :contacts => @contacts ) %>" );
<% else %>
  $('#contacts').html( "<%= escape_javascript( render 'no_contacts', :create_new_link => true ) %>" );
<% end %>
