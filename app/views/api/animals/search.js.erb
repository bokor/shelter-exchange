<% unless @animals.blank? %>
	$('#animals').html( "<%= escape_javascript( render 'api/animals/animal_list', :animals => @animals ) %>" );
<% else %>
	$('#animals').html( "<%= escape_javascript( render 'api/animals/no_animals' ) %>" );
<% end %>
