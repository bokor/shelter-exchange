$('#items_section').hide();
$('#items_section').after( "<%= escape_javascript(render 'items/edit_items', :collection => @shelter) %>" );

// Google Analytics
<% if Rails.env.production? %>
  _gaq.push(['_trackPageview', "/shelters/{id}/edit_wish_list"]);
<% end %>

